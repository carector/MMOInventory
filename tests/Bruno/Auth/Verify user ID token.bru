meta {
  name: Verify user ID token
  type: http
  seq: 5
}

get {
  url: http://localhost:3030/auth/verify
  body: json
  auth: none
}

body:json {
  {
    "idToken": "{{idToken}}"
  }
}

body:multipart-form {
  idToken: {{idToken}}
}
